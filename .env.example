# Supabase (primary data source when both are set)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Legacy Google Apps Script fallback (optional)
VITE_TOOLS_API_URL=

# One-time sheet migration only — never commit, never use in frontend
SUPABASE_SERVICE_ROLE_KEY=
